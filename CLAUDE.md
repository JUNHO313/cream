# CLAUDE.md — 기업 지원용 포트폴리오

이 파일은 AI(Claude)가 이 프로젝트를 도울 때 **먼저 읽는 기준 문서**입니다.
여기 적힌 내용은 실제 코드에서 확인한 사실과, 사용자가 정한 규칙입니다.
확인되지 않은 것은 **"확인 필요"**로 표시하고, 임의로 채우지 않습니다.

---

## 1. 목표

- **용도:** 기업 지원(채용)용 포트폴리오
- **보는 사람:** 채용 담당자, 실무 개발자, 비전공 면접관
- **원하는 인상:** 누구나 쉽게 이해할 수 있으면서 전문적으로 보일 것
- **판단 기준:** 화려함보다 **읽기 쉬움**과 **신뢰**가 우선입니다. 근거 없는 수치나 과장된 표현은 오히려 감점 요소로 봅니다.

## 2. 작업 규칙 (항상 지킬 것)

1. **항상 한국어로 답변합니다.**
2. **모르는 내용은 반드시 먼저 질문합니다.** 경력, 수상, 수치, 기간, 기술 사용 여부를 추측해서 쓰지 않습니다.
3. **없는 내용을 지어내지 않습니다.** 확인 안 된 항목은 `(확인 필요)`로 남기고 사용자에게 묻습니다.
4. **나중에 고치기 쉬운 코드를 씁니다.**
   - 내용(텍스트·데이터)과 화면(구조·스타일)을 분리합니다.
   - 색상·간격은 CSS 변수(`styles.css` 상단)를 사용합니다.
   - 같은 구조가 반복되면 데이터 배열 + 반복 생성으로 처리합니다. (예: `script.js`의 `projectDatabase`)
   - 섹션마다 번호가 붙은 주석으로 구분합니다. (예: `/* 9. PROJECTS FILTERING & MODAL DETAILS */`)
   - 한 번에 한 가지만 수정하고, 수정 전후 화면과 diff를 확인합니다.
5. **손대지 않는 곳:** `smartcity-team1/`(별도 저장소), `학교.txt`(개인정보), `serve.ps1`·`.claude/`(로컬 전용)
6. **커밋은 요청할 때만** 합니다.

## 3. 프로젝트 구조

**프론트엔드와 백엔드가 분리되어 있습니다.** (2026-09-22 작업)

| 폴더/파일 | 역할 |
|---|---|
| `frontend/index.html` | 페이지 뼈대 (섹션 7개). 내용은 비어 있고 JS가 채웁니다 |
| `frontend/styles.css` | 스타일 (색상 변수, 반응형) |
| `frontend/js/main.js` | 진입점 — 무엇을 어떤 순서로 켤지만 정함 |
| `frontend/js/config.js` | 백엔드 주소 설정 |
| `frontend/js/api/` | 서버 통신 (`fetch` 는 여기서만 사용) |
| `frontend/js/ui/` | 화면 장식·공통 도구 (서버와 무관) |
| `frontend/js/features/` | 서버 데이터로 그리는 섹션 (skills, projects, timeline, guestbook, contact) |
| `frontend/admin/` | **관리자 페이지** (`/admin`). 비밀번호 로그인 후 프로젝트 추가·수정 |
| `backend/server.js` | 서버 시작점 |
| `backend/data/content.json` | **포트폴리오 내용 전체** (프로필·스킬·프로젝트·여정) |
| `backend/data/guestbook.json` | 방명록 실제 데이터 (`guestbook.seed.json` = 샘플 원본) |
| `backend/data/contacts.json` | 받은 문의. 개인정보라 `.gitignore` 처리됨 |
| `backend/src/routes/` | 주소 → 컨트롤러 연결 |
| `backend/src/controllers/` | 요청을 응답으로 변환 |
| `backend/src/services/` | 입력 검증·규칙 |
| `backend/src/repositories/` | **데이터 저장 담당. DB를 붙일 때 고치는 유일한 곳** |
| `context.md` | 필터·상세 설명 기능 명세 준비 문서 |
| `serve.ps1` | (구) 정적 서버. 폴더 이동 후 동작하지 않음 — `npm start` 사용 |

### 실행

```bash
cd backend && npm install && npm start   # → http://localhost:3000
```

백엔드가 `frontend/` 를 정적 파일로 함께 제공하므로 명령 하나로 전체가 뜹니다.

### 관리자 페이지 (2026-09-22 추가)

- 주소: `http://localhost:3000/admin`
- 비밀번호: **코드·문서 어디에도 실제 값을 적지 않습니다.** `backend/.env`(gitignore 처리됨, Git에 없음)의 `ADMIN_PASSWORD` 값을 씁니다. `.env` 가 없으면 서버가 임의 생성해 터미널에 1회 표시(해시만 `backend/data/admin-credentials.json` 에 저장, 마찬가지로 gitignore 처리됨).
  - **이 문서(CLAUDE.md)에도 실제 비밀번호 값을 적지 마세요.** 이 파일은 GitHub 공개 저장소에 커밋됩니다. 지금 쓰는 값이 궁금하면 로컬의 `backend/.env` 파일을 직접 여세요.
  - **이 규칙을 되돌리지 마세요.** 공개 저장소에 비밀번호가 노출되는 것을 막기 위한 구조입니다.
  - 지금 `.env` 에 넣어둔 값은 로컬 개발 전용의 짧은 숫자입니다. 인터넷에 배포할 때는 반드시 추측하기 어려운 값으로 바꿔야 합니다.
- **로그인 쿠키는 세션 쿠키입니다 (2026-09-22 변경, `controllers/auth.controller.js`).** `maxAge` 를 절대 추가하지 마세요 — 추가하면 브라우저를 꺼도 로그인이 남는 예전 문제로 되돌아갑니다. 서버 메모리의 12시간 TTL(`config.js` 의 `sessionTtlMs`)은 브라우저를 계속 켜둔 경우를 대비한 이중 안전장치일 뿐, 주된 로그아웃 방식이 아닙니다.
- 관리 항목: 제목 · 내가 한 역할 · 설명 · 날짜 · 참여인원 수 · 참고사항 · 상태(초안/공개)
- **초안**은 빈칸 허용(제목만 필수), 사이트에 안 보임 / **공개**는 참고사항 외 전부 필수, 저장 즉시 반영
- 검증 규칙은 서버(`services/project.service.js`)와 화면(`admin/js/projectForm.js`) 양쪽에 있습니다. **한쪽만 고치면 안 됩니다.**
- 기존 프로젝트 4건은 역할·날짜·참여인원이 비어 있습니다 (**실제 사실이라 임의로 채우지 않음**). 관리자 페이지 목록에 '보완 필요'로 표시됩니다.
- **중복 검사 (2026-09-22 추가):** 저장할 때마다 제목·설명 낱말 겹침 비율(자카드 유사도, `backend/src/utils/textSimilarity.js`)을 계산해 절반 이상 겹치면 목록에 '중복 의심' 배지를 표시합니다. 프로젝트를 열면 상대 항목과 함께 **통합**(빈칸만 채우고 참고사항은 이어붙임) / **상대 삭제** 버튼을 제안합니다. 판단 기준을 하나(겹침 비율)로만 둬서 로직을 단순하게 유지했습니다.

### 수정 규칙 (추가)

- **화면 문구·프로젝트·스킬을 고칠 때는 `backend/data/content.json` 만 고칩니다.** HTML을 건드리지 않습니다.
- 프로젝트는 가급적 관리자 페이지에서 고칩니다. (검증을 거치므로 안전)
- 새 기능이 데이터를 저장해야 하면 `repositories` 에 함수를 추가하고, 서비스는 그 함수만 부릅니다.
- 저장 방식(JSON/DB)은 `DATA_DRIVER` 환경변수로 바꿉니다. 자세한 방법은 `backend/src/repositories/README.md`.

- 저장소: GitHub `JUNHO313/cream`, 브랜치 `main`

### 배포 구조 (2026-09-22 확정)

- **프론트엔드**: Vercel (`https://cream-mu.vercel.app`) — Root Directory `frontend`, 정적 파일만.
- **백엔드**: Render 무료 웹 서비스 — Root Directory `backend`. 서비스 이름을 `cream-backend` 로 만들면
  `frontend/js/config.js` 의 `VERCEL_FRONTEND_BACKEND_URL` 값과 맞습니다. 다르게 만들었다면 그 값을 고쳐야 합니다.
- **왜 나눴나**: Vercel은 서버리스라 파일 저장(JSON)과 서버 메모리 세션(관리자 로그인)을 쓰는
  지금 백엔드를 그대로 못 돌립니다. Render는 계속 켜져 있는 일반 Node 서버라 지금 코드가 그대로 동작합니다.
- **CORS_ORIGIN 을 Render 쪽에 정확히 설정해야 함** (`https://cream-mu.vercel.app`). `*` 로 두면
  로그인 쿠키가 아예 전달되지 않습니다 — `backend/src/app.js` 의 CORS 설정이 출처를 좁혔을 때만
  `credentials`를 허용하도록 되어 있기 때문입니다(의도된 동작, 되돌리지 마세요).
- 로그인 쿠키는 프론트·백엔드가 다른 도메인일 때 자동으로 `SameSite=None; Secure` 로 바뀝니다
  (`backend/src/controllers/auth.controller.js`). 로컬(같은 출처)에서는 기존처럼 `Lax` 그대로입니다.
- Render 무료 티어는 유휴 시 잠들고, 재시작/재배포 시 로컬 파일(JSON) 데이터가 초기화됩니다.
  방문자 데이터를 꾸준히 지키려면 `backend/src/repositories/` 를 실제 DB로 바꿀 시점입니다.
- 자세한 배포 절차는 README.md의 "실제로 배포하기(Vercel + Render)" 참고.

## 4. 포트폴리오 내용 (현재 사이트 기준)

### 4-1. 기본 정보
- 이름: 장준호 (Jang Junho)
- 전공: 그린스마트시티학과 / 학번 202220824 / 2022.03 입학
- 지역: 인천광역시
- 이메일: junho122009@naver.com
- 한 줄 소개: "기술과 환경이 조화를 이루는 스마트 시티를 꿈꿉니다."

### 4-2. 섹션 구성

| # | 섹션 id | 제목 | 내용 |
|---|---|---|---|
| 1 | `hero` | 첫 화면 | "지속 가능한 미래 도시를 만드는 그린 스마트시티 개발자" |
| 2 | `about` | 소개 | 프로필 카드 + 강점 3가지 |
| 3 | `skills` | 역량 | 기술 6개 (분류 탭: 전체/스마트시티/프론트엔드/백엔드) |
| 4 | `projects` | 프로젝트 | 카드 4개 + 분류 필터 + 상세 모달 |
| 5 | `timeline` | 여정 | 학력·활동 4건 |
| 6 | `guestbook` | 방명록 | 브라우저 저장(localStorage) 기반 |
| 7 | `contact` | 연락처 | 이메일·학과·지역·GitHub + 메시지 폼 |

### 4-3. 강점 3가지 (about)
1. 그린 스마트시티 & 친환경 인프라 이해도
2. 도시 공간 데이터(GIS) & 대시보드 시각화
3. 현대적인 풀스택 소프트웨어 개발 역량

### 4-4. 기술 스택 (skills)

| 기술 | 분류 | 표기된 숙련도 |
|---|---|---|
| 스마트시티 IoT & 센서 데이터 | smartcity | 능숙함 |
| 그린 테크 & 에너지 효율 분석 | smartcity | 능숙함 |
| React / Next.js & GIS Map | frontend | 능숙함 |
| JavaScript & TypeScript | frontend | 능숙함 |
| Python & 도시 데이터 분석 | backend | 활용 가능 |
| Node.js & PostgreSQL (PostGIS) | backend | 활용 가능 |

### 4-5. 프로젝트 4건

| 카드 id | 제목 | 분류 | 주요 기술 |
|---|---|---|---|
| citypulse | CityPulse — 실시간 스마트시티 환경 관제 대시보드 | smartcity | React, Leaflet, FastAPI, MQTT, PostGIS |
| greencampus | GreenCampus — 스마트 친환경 캠퍼스 허브 | green | Next.js, TypeScript, PostgreSQL, Prisma |
| ecocycle | EcoCycle — 제로웨이스트 & 탄소 중립 커뮤니티 | green | React, Express, MySQL, AWS S3, Docker |
| devorbit | DevOrbit — 연구팀 실시간 협업 플랫폼 | web | React, Node.js, Socket.io, MongoDB |

- 카드 내용: `index.html` 585행~ / 상세 모달 내용: `script.js` 537–593행(`projectDatabase`)
- 상세 모달 표시 항목: 개요 · 사용 기술 · 주요 구현 기능

### 4-6. 여정 (timeline)
1. 2022.03 – 현재: 그린스마트시티학과 입학
2. 2023.08: 스마트시티 아이디어톤 & 해커톤 우수상
3. 2024.04 – 2024.12: GIS & IoT 관제 대시보드 프로젝트 (CityPulse)
4. 2025 – 현재: 차세대 그린 스마트시티 웹 솔루션 연구

## 5. PDF 출력 계획

- 최종 산출물은 **PDF**이며, **페이지 단위로 내용을 나눠 관리**합니다.
- 페이지 구성안 (초안, 확정 전)

  | 페이지 | 내용 |
  |---|---|
  | 1 | 표지 — 이름 / 전공 / 한 줄 소개 / 연락처 |
  | 2 | 소개 & 강점 3가지 |
  | 3 | 기술 스택 |
  | 4–7 | 프로젝트 1건당 1페이지 (개요·역할·기술·결과) |
  | 8 | 여정(학력·활동) |
  | 9 | 연락처 |

- PDF를 염두에 둔 작업 원칙
  - 한 페이지에 들어갈 분량 단위로 내용을 끊어서 작성합니다.
  - 인쇄 시 색이 빠져도 읽히도록, 정보를 **색으로만** 구분하지 않습니다.
  - 마우스를 올려야 보이는 내용, 클릭해야 열리는 내용(상세 모달 등)은 PDF에서 사라지므로 **인쇄용으로는 펼쳐진 형태**가 필요합니다.
  - 배경 애니메이션·파티클은 인쇄 대상이 아닙니다.

- **확인 필요:** PDF를 만드는 방식(브라우저 인쇄용 CSS `@media print` / 별도 PDF 전용 페이지 / 다른 도구) — 사용자 결정 후 진행

## 6. 도메인 기준 (확인 필요)

메시지에서 일부 내용이 끊겨서 정확히 받아 적지 못했습니다. 아래는 **확정 전**이며, 답을 받은 뒤 이 문서에 채웁니다.

| 항목 | 현재 상태 |
|---|---|
| 참고할 용어집 (기관명) | **확인 필요** — 어느 기관의 용어집인지 |
| 사용 데이터 (토지정보 관련) | **확인 필요** — 데이터 이름·출처·공개 여부 |

정해지면 규칙: 도메인 용어는 해당 용어집 표기를 따르고, 데이터 출처는 프로젝트 설명에 함께 적습니다.

## 7. 사실 확인이 필요한 항목

기업 지원용이므로 **실제와 다른 내용이 있으면 위험**합니다. 아래는 코드에서 발견한, 확인이 필요한 부분입니다.

1. **프로젝트 성과 표현** — "초당 수천 건의 센서 데이터", "Lambda 리사이징", "Jest 유닛 테스트", "PWA 탑재" 등이 실제로 구현한 내용인지
2. **프로젝트 실물 여부** — 카드의 GitHub·데모 버튼은 현재 실제 주소 없이 안내 메시지만 표시됩니다. 공개 저장소나 데모가 있는지
3. **GitHub 주소 불일치** — 화면에는 `github.com/junho-jang`, 실제 저장소는 `github.com/JUNHO313` 입니다. 어느 쪽이 맞는지
4. **수상 내역** — "해커톤 우수상"의 정확한 대회명·주최·일자
5. **숙련도 표기** — "능숙함 / 활용 가능" 기준
6. **프로필 사진** — `profile.jpg` 파일이 없어 404가 납니다. 사진을 넣을지
7. **개인정보 공개 범위** — 학번·이메일을 공개 사이트와 PDF에 그대로 둘지

## 8. 진행 중인 작업

- `index.html` 페이지 제목에서 학번 `(202220824)` 제거 (실습 02, 커밋 전)
- `context.md` 미정 조건: 필터 4건 결정 완료(6장 기능 명세로 정리) / 상세 설명 4건(#5–#8) 결정 대기
- 다음 단계: `context.md` 6장 명세대로 `index.html`·`script.js`·`styles.css`에 분야별 필터 적용
