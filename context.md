# context.md — 내 웹사이트 (포트폴리오)

> AI에게 작업을 맡기기 전에 먼저 읽히는 배경 설명서입니다.
> 여기 적힌 내용은 **현재 코드에서 확인한 사실**과 **작업 규칙**만 담습니다.
> 아직 정하지 않은 조건은 "미정"으로 두고, 임의로 채우지 않습니다.

## 1. 프로젝트 개요
- **무엇:** 그린스마트시티학과 학생의 개인 포트폴리오 웹사이트 (한 페이지)
- **저장소:** GitHub `JUNHO313/cream`, 브랜치 `main`
- **구성:** 빌드 없는 정적 사이트
  - `index.html` — 화면 구조 (섹션: hero, about, skills, projects, timeline, guestbook, contact)
  - `script.js` — 동작 (필터, 모달, 방명록, 테마 등)
  - `styles.css` — 스타일
- **로컬 미리보기:** PowerShell에서 `./serve.ps1` 실행 → http://localhost:8000

## 2. 실습 대상 기능 (현재 상태)

### 2-1. 프로젝트 필터
- 위치: `index.html` 574–579행 (버튼), `script.js` 595–624행 (`initProjectFilters`)
- 버튼 4개: `all`(전체보기) · `smartcity`(스마트시티 & IoT) · `green`(그린 캠퍼스 & 친환경) · `web`(웹 플랫폼)
- 카드 4개와 분류

  | 카드 id | 제목 | data-category |
  |---|---|---|
  | citypulse | CityPulse | smartcity |
  | greencampus | GreenCampus | green |
  | ecocycle | EcoCycle | green |
  | devorbit | DevOrbit | web |

- 동작: 버튼 클릭 → 해당 버튼만 `active` → 분류가 맞는 카드만 보이고 나머지는 0.25초 뒤 숨김
- 선택 상태는 저장하지 않음 (새로고침하면 "전체보기"로 돌아감)

### 2-2. 상세 설명 (프로젝트 모달)
- 위치: `index.html` 1079행~ (`#project-modal`), `script.js` 537–593행 (`projectDatabase`), 626–676행 (열기/닫기)
- 열기: 카드의 **상세보기** 버튼 → `openProjectModal(id)`
- 표시 내용: 분류, 제목, 설명(`desc`), 기술 목록(`tech`), 주요 기능(`features`)
- 닫기: 닫기(X) 버튼 · 닫기 버튼 · 바깥 영역 클릭 · `Esc` 키
- 열려 있는 동안 페이지 스크롤 잠김
- GitHub / 라이브 데모 링크는 실제 주소 없이 안내 메시지(toast)만 띄움

## 3. 원문(자료) 규칙
- 카드와 상세 설명의 **원문 기준은 `자료.md`** 입니다. (아직 이 폴더에 없음 — 받으면 같은 폴더에 둠)
- **원문에 없는 성과·수치·기능을 추가하지 않습니다.** 발견하면 기능 구현보다 조건 수정이 먼저입니다.
- 현재 `projectDatabase`와 카드 문구는 **원문 대조 전**입니다.
  예: "초당 수천 건의 센서 데이터", "Lambda 리사이징", "Jest 유닛 테스트" 같은 표현은 `자료.md`로 사실 확인이 필요합니다.

## 4. 작업 규칙
- **실습 04(기능 명세 준비) 단계에서는 파일을 수정하지 않습니다.** 읽고, 표로 정리하고, 질문만 합니다.
- 수정 단계에서는 한 번에 한 건씩, **프롬프트 · 수정 전후 화면 · Diff**를 남깁니다.
- 수정하지 않는 곳
  - `smartcity-team1/` — 별도 저장소
  - `학교.txt` — 개인정보 (git 제외)
  - `serve.ps1`, `.claude/` — 로컬 전용
- 커밋은 요청할 때만 합니다.

## 5. 미정 조건 (결정 필요)
필터·상세 설명 명세표를 만들기 전에 정해야 할 것들입니다.

| # | 기능 | 질문 | 결정 |
|---|---|---|---|
| 1 | 필터 | 새로고침·뒤로가기 후에도 선택한 필터를 유지할까? | **유지하지 않음** (항상 `전체보기`로 시작) |
| 2 | 필터 | 결과가 0개인 분류가 생기면 안내 문구를 보여줄까? | **보여줌** |
| 3 | 필터 | 분류 이름·개수(4개)는 `자료.md` 기준으로 바꿀까? | **현재 분류 그대로 진행** (전체 + smartcity/green/web) |
| 4 | 필터 | 필터를 바꾸면 열려 있던 상세 설명은 닫을까? | **자동으로 닫음** |
| 5 | 상세 설명 | 표시 항목(설명·기술·기능) 외에 원문에 있는 항목(기간, 역할 등)을 넣을까? | 미정 |
| 6 | 상세 설명 | 닫은 뒤 키보드 포커스를 "상세보기" 버튼으로 돌려줄까? | 미정 |
| 7 | 상세 설명 | GitHub / 데모 링크가 원문에 없으면 버튼을 숨길까? | 미정 |
| 8 | 공통 | 원문과 다른 현재 문구는 원문대로 고칠까, 원문에 추가할까? | 미정 |

## 6. 분야별 필터 — 기능 명세 (확정)

5번 표의 1~4번 결정을 그대로 옮긴 것입니다. 구현은 이 표 밖으로 나가지 않습니다.

### 6-1. 동작 조건

| # | 조건 | 내용 |
|---|---|---|
| F1 | 첫 화면 | 항상 `전체보기`가 선택된 상태. 카드 4개 모두 표시 |
| F2 | 버튼 클릭 | 누른 버튼 하나만 `active`, 나머지는 해제 |
| F3 | 카드 표시 | `data-category`가 맞는 카드만 보이고 나머지는 숨김 |
| F4 | 결과 개수 | 현재 보이는 카드 수를 문구로 표시 (예: `프로젝트 2개`) |
| F5 | 결과 0개 | 카드 자리에 안내 문구 표시 — "해당 분야의 프로젝트가 아직 없습니다." |
| F6 | 상태 저장 | 저장하지 않음. 새로고침·뒤로가기 시 `전체보기`로 복귀 |
| F7 | 모달 | 필터를 바꾸면 열려 있던 상세 모달을 닫음 (스크롤 잠금도 함께 해제) |
| F8 | 유지할 것 | 카드 문구·순서·링크·모바일 배치는 현재 그대로 |

### 6-2. 분류와 카드 (현재 기준, 변경 없음)

| 버튼 | 값 | 해당 카드 | 개수 |
|---|---|---|---|
| 전체보기 | `all` | 전부 | 4 |
| 스마트시티 & IoT | `smartcity` | CityPulse | 1 |
| 그린 캠퍼스 & 친환경 | `green` | GreenCampus, EcoCycle | 2 |
| 웹 플랫폼 | `web` | DevOrbit | 1 |

### 6-3. 디자인 기준

- 선택 상태를 **색으로만** 구분하지 않습니다. 배경색 + 글자 굵기(또는 테두리)를 함께 바꿉니다. → 인쇄·PDF에서 흑백이 되어도 어느 버튼이 눌렸는지 보이게 하기 위함 (CLAUDE.md 5장)
- 색·간격은 `styles.css` 상단의 CSS 변수를 사용하고, 새 색상 값을 직접 적지 않습니다.
- 결과 개수 문구와 빈 결과 안내 문구는 본문보다 작고 연한 글자로, 카드 위/카드 자리에 둡니다.
- 모바일에서 버튼이 4개 다 들어가지 않으면 줄바꿈합니다. (가로 스크롤 금지)
- 카드가 사라질 때 애니메이션은 현재 동작(0.25초)을 유지합니다.

### 6-4. 확인 순서 (수정 후)

1. 첫 화면에서 `전체보기` 선택 + 카드 4개 표시 확인
2. `그린` 클릭 → 카드 2개(GreenCampus, EcoCycle) + 문구 `프로젝트 2개` 확인
3. 상세 모달을 연 상태로 다른 필터 클릭 → 모달이 닫히는지 확인
4. 새로고침 → `전체보기`로 돌아오는지 확인
5. 모바일 폭(약 375px)에서 버튼 줄바꿈과 카드 배치 확인

**중단 기준:** 필터 외의 문구·순서·링크·모바일 배치가 바뀌면 다음 작업으로 넘어가기 전에 되돌립니다.
